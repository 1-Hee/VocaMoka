<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.VokaMoka" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- 배경 -->
        <item name="android:colorBackground">@color/mono800</item>
        <item name="colorSurface">@color/mono800</item>

        <!-- 주요 색상 -->
        <item name="colorPrimary">@color/primary_variant</item>
        <item name="colorPrimaryVariant">@color/primary</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- 보조 색상 -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- 포인트 -->
        <item name="colorAccent">@color/accent</item>
        <item name="colorTertiary">@color/tint_optional</item>

        <!-- 상태 색상 -->
        <!--
        <item name="colorSuccess">@color/success</item>
        <item name="colorError">@color/error</item>
        -->

        <!-- 텍스트 & 경계선 -->
        <!--
        <item name="textColorPrimary">@color/white</item>
        <item name="textColorSecondary">@color/mono400</item>
        -->
        <item name="android:colorForeground">@color/white</item>
        <item name="colorOutline">@color/mono600</item>
    </style>
</resources>